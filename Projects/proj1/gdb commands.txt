set confirm off 
delete

def xz
python for i in range(gdb.parse_and_eval('m_size')): gdb.execute("p *m_cols[" + str(i) + "]");gdb.execute("p *m_cols[" + str(i) + "].m_data@" + str(gdb.parse_and_eval('m_capacity')))
p *m_colInfo@m_capacity*2+1
end


define zx
undisp
disp *this
python for i in range(gdb.parse_and_eval('m_size')): gdb.execute("disp *m_cols[" + str(i) + "]");gdb.execute("disp *m_cols[" + str(i) + "].m_data@" + str(gdb.parse_and_eval('m_capacity')))
disp *m_colInfo@m_capacity*2+1
end

# py gdb.execute("b SortedSquareList.cpp:%s" % str([i for i,j in enumerate(open("SortedSquareList.cpp")) if ("// Determine the Square Dimension of the adjacent columns ") in j][0]))
# commands
# disp {left,right}
# end

# b SortedSquareList::remove(int)
# commands
# silent
# zx
# c
# end

def xc
tb SortedSquareList::shiftFromLeft(int)
tb SortedSquareList::shiftFromRight(int)
c
end

tb main
commands
silent
call dup2(open("input.txt", 0), 0)
continue
end


define cv
source -s gdb commands.txt
end

r